@*<div class="collapse navbar-collapse">*@
    <ul class="navbar-nav navbar-right">
        @if (Session["User"] == null)
        {
            <li class="nav-item active"><a class="nav-link" href="@Url.Action("LogOff", "Account")">登录</a></li>
        }
        else
        {
            <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "User")">Hi，@(((UserEntity)Session["User"]).NickName)</a></li>
            <li class="nav-item active"><a class="nav-link" href="@Url.Action("LogOff", "Account")">注销</a></li>
        }
    </ul>
@*</div>*@